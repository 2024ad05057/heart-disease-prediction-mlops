apiVersion: v1
kind: ConfigMap

metadata:
  name: prometheus-config
  namespace: heart-disease

data:
  prometheus.yml: |
    global:
      scrape_interval: 15s

    scrape_configs:
      - job_name: "heart-api"

        metrics_path: /metrics

        static_configs:
          - targets:
              - heart-disease-service:8000